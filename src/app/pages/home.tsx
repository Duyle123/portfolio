//========= Import Bootstrap ==============
import {gsap, Power3} from 'gsap';

import React from 'react';

function HomePage() {
    return (
        <div className="home">
            homepage
        </div>
    );
}

export default HomePage;